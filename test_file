void init_editor() {
	if (getWindowSize(&editor.screen_rows, &editor.screen_cols) == -1)
	d	panic(getWindowSize");

	editor.rows = editor.screen_rows;
	editor.cols = editor.screen_cols - editor.margins;
}